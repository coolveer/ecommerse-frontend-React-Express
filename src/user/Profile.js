import React from 'react';
import  Base  from "../core/Base";

const Profile = () => {
    return(
        <Base title="user dashboard">
        <h1> this is a user Profile dashboard </h1>
        </Base>
    )
}

export default Profile;